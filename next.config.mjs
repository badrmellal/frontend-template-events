/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ['images.unsplash.com', 'i.postimg.cc', 'images.pexels.com', 'cdn.pixabay.com', 'africa-events.s3.eu-west-3.amazonaws.com'],
    }
};

export default nextConfig;
